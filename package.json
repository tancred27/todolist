{
	"name": "to-do-list",
	"version": "1.0.0",
	"description": "a simple to-do list app made with react",
	"main": "server.js",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"express": "^4.17.1",
		"mysql": "^2.18.1"
	},
	"devDependencies": {
		"concurrently": "^5.2.0",
		"nodemon": "^2.0.4"
	},
	"scripts": {
		"start": "node server.js",
		"server": "nodemon server.js",
		"client": "npm start --prefix client",
		"clientinstall": "npm install --prefix client",
		"dev": "concurrently \"npm run server\" \"npm run client\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Vardhan",
	"license": "ISC"
}
